const Title = ({className}) => {;
	return (
		<div className={className}>
            <h2>
                <span>/</span>
                reviews
            </h2>
        </div>
	);
}

export default Title;